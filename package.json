{
  "name": "agenda",
  "version": "1.0.0",
  "description": "",
  "main": "./src/app.js",
  "scripts": {
    "server": "nodemon --experimental-modules ./src/app.js",
    "start": "node --experimental-modules ./src/app.js",
    "heroku-postbuild": "cd client && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Leandro",
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "cors": "^2.8.5",
    "create-react-app": "^3.4.1",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.9.27"
  },
  "nodemonConfig": {
    "ignore": [
      "client/"
    ]
  },
  "engines": {
    "node": "*"
  }
}
